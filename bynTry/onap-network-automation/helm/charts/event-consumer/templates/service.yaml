apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-event-consumer
  labels:
    app: event-consumer
spec:
  selector:
    app: event-consumer
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
